# Prompt Goblin — PLAN (living)

> The orchestration doc. The main thread keeps this current; subagents are dispatched from here. This is the "planner" — there is no planner subagent. Last touched: 2026-06-13.

## ⚠️ Deploy note

`main` is **deploy-on-push** — pushing `main` rebuilds + ships the **live `web/` Next.js site**. **As of 2026-06-08 the DO app is a Node/SSR Web Service** (converted from `static_sites`; spec in `.do/app.yaml`, `npm run start`, http_port 8080) — the dashboard cutover is **DONE + LIVE**. Marketing routes still prerender to static HTML inside the Node app; `/dashboard`, `/runs*`, `/login`, `/auth/*` are dynamic. **Do NOT re-add `output:'export'`** (breaks the dashboard + deploy). Heads-up: under unified deploy-on-push, even a doc-only push to `main` triggers a full rebuild. Gate every merge (tests + build + required reviewers) before pushing.

## ✅ Current state — 2026-06-13 (Claude session — payments testing + site UX/copy)

**A. Stripe payment-gateway testing (shipped + verified).**
- **Behavioral webhook suite** `web/__tests__/stripe-webhook.behavior.test.ts` (17 tests): executes the real `POST` handler with genuinely Stripe-signed payloads + an in-memory Supabase fake — signature reject (missing/wrong-secret/tampered), paid/unpaid checkout, idempotency (dup + in-flight 409), Resend best-effort, cross-tenant refusal, subscription lifecycle, secondary-secret fallback. (Prior "tests" were source-string greps only.)
- **Multi-secret webhook verification** in `route.ts` (`getStripe()` → `webhookSecrets[]`, first match wins) — enables a parallel test-mode endpoint + zero-downtime secret rotation. Inert unless `STRIPE_WEBHOOK_SECRET_TEST` is set.
- **Runbook** `web/docs/stripe-testing.md` (CI behavioral · Stripe CLI local e2e · manual pre-launch checklist).
- **Live Payment Links fixed:** scout/warband/warlord (live + test) were missing the required `domain` custom field + `plan` metadata → silent provisioning failure. All 8 links now collect `domain` + carry `plan`, and redirect to `/welcome` on completion. **Metadata→session propagation confirmed; zero real purchases had completed, so no customer was hit.** See memory `promptgoblin-stripe-links-config`.
- **All 4 plans validated** end-to-end via `stripe listen` (correct plan→tier mapping; test users cleaned up). A standing prod test-mode endpoint was created, used, then **torn down (GA teardown done)** — prod verifies live-secret only.

**B. Site UX/copy batch (shipped + verified, 2 deploys: `e057e43` + this push).**
- **Demo flow:** `VISIBLE AF` badge → **Book a demo** CTA; Summon/Book-a-demo CTAs now open the **SummonForm in an accessible modal** (Zustand `openSummon(demo?)`, focus-trap/Esc/backdrop, `SummonModal`); demo entry pre-checks the demo box. Inline Contact form kept as no-JS fallback.
- **Summon form:** single submit button **"summon a goblin"** (removed the separate "run my free scan" + Stripe ghost link).
- **Auth-aware header:** logged-in shows a hamburger (Dashboard + Sign out); logged-out keeps the login modal (client-side check — marketing stays SSG).
- **Post-purchase `/welcome` page** (branded; set as success_url on all 8 Stripe links).
- **Money-back guarantee removed** (Pricing, Contact, FAQ + 2 stale metadata refs) per owner.
- **Dashboard upgrade copy** → links to pricing, "monthly, cancel anytime" (proration claim rejected by integrity — Payment Links create a NEW sub, no auto-proration; a real prorated upgrade flow is a separate TODO).
- **llms.txt neutralized** (file + `/learn/llms-txt-implementation` + JSON-LD): dropped the "no-evidence / not-a-lever / Google-dismissal" editorializing → neutral "some AI agents read it in-session, many ignore it." Honest-broker intact (no citation-lever claim). integrity APPROVE.
- **Responsive:** hero scan fully in-viewport on <4K laptops (1366×768 + 1440×900 verified); Spellbook mobile border clip fixed; dashboard nav mobile-safe (≥44px targets); `/auth/confirm` restyled to the terminal/HUD aesthetic.
- **Gates:** web lint 0; vitest green (incl. 17 behavioral + content-pages); integrity-reviewer APPROVE ×3 (copy, prorate fix, llms.txt). LiveScan isn't on the homepage (the "scan" is the Hero scan).
- **TODO (owner):** eyeball logged-in hamburger + dashboard mobile + `/auth/confirm` on real sessions; decide on a true prorated in-app upgrade flow; the demo "within one working day" promise is a hard commitment (soften if needed).

## ✅ Current state — 2026-06-12 (Claude session — own-site visibility)

- **Reality check:** Google doesn't surface promptgoblin.io for "ai search visibility" (head term owned by Semrush/SE Ranking/HubSpot — out of reach for now); `site:promptgoblin.io` ≈ zero results (authority/age, NOT a technical block — robots/sitemap verified fine); brand SERP for "promptgoblin" is owned by the unrelated promptgoblin.com prompt-generator tool.
- ✅ **GSC + Bing Webmaster Tools DONE** (owner, confirmed 2026-06-12): properties verified + sitemaps submitted. Removed from the owner-gated list below.
- **🆕 Spec: `specs/longtail-page-targets.md`** — brand-query rescue (crawlable disclosed mentions tying "Prompt Goblin" → promptgoblin.io) + prioritized long-tail page targets + page-shape contract + GSC/Bing measurement loop. Drafted via research workflow (4 researchers → draft → integrity + completeness gates); implement directly (no Codex handoff).
- **Long-tail batch deployed on `main` (`21e19a8`, live 2026-06-12):** Batch 1 pages `/learn/why-schema-not-enough`, `/learn/aeo-audit-checklist`, and `/learn/rank-but-not-cited` shipped earlier on the branch; takeover also shipped `/learn/eeat-for-ai-search`, `/learn/entity-clarity-for-ai`, and `/learn/llms-txt-implementation`. Page-shape contract honored (direct-answer open, tables where natural, single-sourced FAQ + FAQPage JSON-LD mapped from the same `FAQ_ITEMS` arrays, sources, "what this does not guarantee", scan CTA, at least 3 internal links). Plumbing done: sitemap, llms.txt x2 (web/public + root), term-map rows, IndexNow key file. **Gates green: integrity-reviewer APPROVE, web lint 0, vitest 107/107, build green (30 routes), local production smoke SSR/JSON-LD/canonical/internal links, axe-core 0 violations x 6 scans (3 routes x desktop/mobile).** Live verified 2026-06-12: all three new takeover routes return 200 with SSR article content + JSON-LD; live sitemap and live llms.txt include all three; `/indexnow` returned HTTP 200 for the submitted URL list.
- **Measurement table (per spec):**
  | URL | IndexNow submitted | 2-wk Bing check | 4-wk GSC check |
  |---|---|---|---|
  | `https://promptgoblin.io/learn/eeat-for-ai-search` | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/learn/entity-clarity-for-ai` | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/learn/llms-txt-implementation` | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/learn/why-schema-not-enough` | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/learn/aeo-audit-checklist` | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/learn/rank-but-not-cited` | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/learn/faq-schema-vs-howto-schema` | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/learn/site-structure-ai-citations` | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/learn/wcag-aeo-overlap` | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/docs/bing-webmaster-tools` (upgraded) | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/benchmark` (upgraded) | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/faq` (upgraded) | 2026-06-12 | 2026-06-26 | 2026-07-10 |
  | `https://promptgoblin.io/docs/report-guide` (upgraded) | 2026-06-12 | 2026-06-26 | 2026-07-10 |
- **✅ Spec COMPLETE — batch 3 + upgrades + P0 pack shipped (`77f8681`, pushed 2026-06-12):** `/learn/faq-schema-vs-howto-schema` (2023 rich-results deprecation, mobile-Aug/desktop-Sep precision) · `/learn/site-structure-ai-citations` (hub-and-spoke/passage retrieval) · `/learn/wcag-aeo-overlap` (Section 508 procurement; 508=WCAG 2.0 AA precision; pre-ship cannibalization grep clean). Upgrades: `/docs/bing-webmaster-tools` stub→full guide (TechArticle JSON-LD, not-a-guarantee line intact) · `/benchmark` context links + OG description de-overclaimed · `/faq` page-level `faqPageJsonLd` (separate export, homepage 6-block graph untouched) + Learn-more links · report-guide/methodology/aeo-vs-geo/technical-seo cross-links per §5. **`marketing/brand-mention-drafts.md`** (P0): integrity-gated APPROVE (Sortlist entry-price + BBB locality fixed); human-submit only; HARO/Reddit filled instances need re-gate. Earlier same day: copy pass `7c9f696` dropped the "days, not quarters" timeline promise. **Gates green: per-lane integrity (4× REVISE→fixed, 2× APPROVE), lint 0, vitest 117/117, build green, axe 0 × 28 scans.** **Live-verified 2026-06-12:** all 3 new routes + 4 upgraded routes return 200 with SSR content + JSON-LD; live sitemap + llms.txt include them; benchmark OG description confirmed fixed on live; IndexNow returned 200 for the 7-URL list (rows above). All 9 spec pages are now LIVE; remaining spec work is the ongoing measurement loop + owner mention submissions. Deliberate deviation: breadcrumb "Learn" crumbs keep the site-wide house pattern (point at the page's own URL — there is no `/learn` index route); a site-wide breadcrumb redesign is a separate task if wanted.
- **Coordination change (owner call):** the **Codex handoff lane is retired** — Claude is the standing integrator + implementer. `COORDINATION.md`, `CLAUDE.md`, and `specs/INDEX.md` updated; `feedback/codex/` kept as historical record.

## ✅ Current state — 2026-06-11 (Claude session #2 — implement fleet)

> Branch `fix/scan-citation-label` = `origin/main` HEAD (`b6e4fe2`). **The money path IS merged + deployed** — the older "pending merge" note below was stale (local `main` was 17 commits behind origin). New fleet work below is UNCOMMITTED in the working tree, awaiting owner review.

- **5-lane gated agent fleet shipped (outreach-readiness batch), uncommitted on `fix/scan-citation-label`:**
  - **Pipeline (graph-keeper APPROVE, pytest 327 / eval 3/3):** I-06 fixed — `upsert_run` hard-refuses `mode=='mock'`/`is_sample` writes (code-level, can't be re-enabled by ambient env) + pytest `conftest` + `run_eval` env-clamp; competitor acceptance #22 (walgreens→CVS-class recall + junk-domain rejection + vertical-hint fallback flagged inference/verify); term-map expansion node #16 + Top-5 tracked queries.
  - **Live `runs` table PURGED (MCP):** 1,478 mock runs + 51,365 mock recs deleted; **2 real `live` dogfood runs preserved** (still `approved=false`, awaiting honest review). Dashboard is clean; I-06 guard stops refill.
  - **Functions (npm test 242):** Scrapfly WAF bypass hardened (`country=us`, `rendering_wait`, 2nd attempt on residential pool, all within the 30s ASP budget) + meijer-class honest-blind-spot regression lock. _Real bypass success still needs a live keyed probe — not claimed._
  - **Web content (integrity APPROVE):** homepage **3-levers section** #18 (the causal model in goblin voice, schema=hygiene explicit) + 6 learn pages deepened from ~40-line stubs to real answer-shaped content #19 + Article/FAQ JSON-LD.
  - **Web ux/email (integrity REVISE→fixed):** Loader blink 1300→420ms #6; branded welcome email #9/A-15; 5 tracked queries surfaced in scan UI #17; honest inline scan-email CTA + new `/api/scan-email` #4/#14 (the false "engineer follows up" no-mailer promise was removed by the gate; `queued`→`delivered:false`).
  - **Web dashboard/auth:** modal login #10/A-07; 3-step onboarding checklist #12; tier-gate verified #11.
  - **Main-thread cleanup:** 9 lint errors fixed (unescaped `<code>` quotes ×7 + onboarding setState-in-effect); **removed the fabricated "50 prompts × 4 engines"** homepage teaser number (no "50" exists in backend — real config is max_queries 8 / teaser 2). **web lint 0 + build 0; all 33 routes compile incl. new `/api/scan-email`.**
- **⚠️ Still owner-gated before outreach:** rotate secrets · DO env (Stripe/Resend/`NEXT_PUBLIC_SITE_URL`) · Resend account + DNS (SPF/DKIM/DMARC) · Supabase custom SMTP · live $1 e2e · ~~GSC/Bing sitemap submit~~ ✅ done 2026-06-12 · **live dogfood re-run + honest approve** (the one artifact that lets "proven by our own scan" appear in a DM). And: review + merge this branch (deploy-on-push ships it).
- **Pre-existing honest-broker flag for the next copy pass:** the free Tier-2 "✓ cited" teaser still reads brand-self-citation as visibility — Option A wording proposed in `feedback/claude/2026-06-11-citation-teaser-overstatement.md`, owner + integrity sign-off pending.

## ✅ Current state — 2026-06-11 (Claude session — money path)

> Branch `fix/indexnow-canonicals` (6 commits ahead of `main`), gated, pending merge. _(Superseded: this work is now ON origin/main — see session #2 note above.)_

- **Stripe money path COMMITTED + HARDENED** (`e86c36a` build, `e49caa8` hardening): `web/app/api/webhooks/stripe/route.ts` (idempotent `stripe_events` ledger · `payment_status==='paid'` · `admin.createUser` · `provision_stripe_checkout_client` RPC · `generateLink` hashed_token · Resend best-effort) + `/auth/confirm` SafeLinks interstitial (`verifyOtp`, not PKCE) + `/api/runs` explicit auth gate. Pre-merge **integrity-reviewer** (REVISE→fixed) + **security review** (DO-NOT-SHIP→fixed): **C1** host-header magic-link phishing, **H1** email-failure customer loss, **H2** stuck-`processing` events, **M1** cross-tenant domain seizure, + M3/L1. 4 regression-lock tests added. **Gate green: web vitest 82/82 + build; pipeline 305 pytest + eval 3/3.**
- **Migration `0015_stripe_events` APPLIED LIVE + verified** (MCP): billing columns + `welcome_email_status` on `clients`, service-role-only `stripe_events` (RLS forced, no policy = deny-all by design), RPC `revoke public/anon/authenticated` + `grant service_role`. Advisors: no new RLS gaps (only the known `vector`-in-public + leaked-password WARNs).
- **IndexNow + Bing**: real key + `keyLocation` + own-host validation live in code (`0a812f4`); Bing verification via msvalidate meta + `web/public/BingSiteAuth.xml`. Owner does GSC/Bing onboarding.
- **Dogfood**: recon/listen fix already committed + pushed (`024737d`, pipeline `master`); the old bad run is **not** approved (live `approved=true` count = 0 — integrity issue already clear).
- **⚠️ NEW: live `runs` table polluted** — 1441 rows because `goblin.eval`/pytest write to the live dogfood client (`GOBLIN_SUPABASE_ENABLED=true`). Needs eval/test isolation + cleanup (TODO `I-06`).
- **Open owner blockers before money path goes live:** rotate secrets · set DO env (`NEXT_PUBLIC_SITE_URL` + Stripe/Resend secrets, `I-07`) · Stripe Payment Links must collect `domain` (`I-08`) · Resend DNS + click-tracking OFF · live $1 e2e.

## ✅ Current state — 2026-06-06 (Claude-integrator session)

> Authoritative snapshot; older "In flight / Queued" entries below may lag.

- **Coordination:** owner moved to **Claude as the sole integrator** for this push (Codex stood down; no shared-tree multi-writer). Hermes's vault lane unchanged.
- **Marketing site:** `web/` Next.js cutover **DONE + live** on promptgoblin.io (DO builds `web/`→`out/` on push to `main`); old root SPA retired.
- **Wave 1 — shipped + gated:** pipeline (freshness · topical-authority · per-platform lanes · third-party presence · citation-verification offline) — graph-keeper re-gate **APPROVE**, **289 pytest + eval 3/3**, merged to pipeline `master` + pushed (`061bbc9`). Web/functions (`/methodology` · `/learn/aeo-vs-geo` · LiveScan Tier-2 prune · functions citation-verifier) merged to `main` + deployed. Functions redeployed (Scrapfly WAF-bypass + render-diff + buildRenderDiff fix); **live walgreens.com probe honest** (real score 13, `staticWasBlocked:true`, never 0).
- **Wave 2 — schema (#4): ✅ APPLIED LIVE** (2026-06-06, via the Supabase MCP connector — the `.env` route was blocked by a `[YOUR-PASSWORD]` placeholder, so we used OAuth-based MCP instead). Migrations `0001–0009` in the live ledger; verified: **10 public tables, RLS enabled+forced on all 10, 40 policies + the storage policy, the ivfflat `vector(1536)` index, and the private `scan-proof` bucket.** Security advisors: no RLS gaps; one known low-priority WARN (`vector` extension in `public` — standard Supabase default, left as-is). Added `0009_harden_function_search_path` to silence the function-search-path advisor.
- **Wave 2 — dashboard (#5): ✅ MERGED + LIVE** (2026-06-08). `web/`→Node-SSR + dashboard MVP shipped to `main` and the live DO app (cutover via MCP, preview-validated). All gates green: pipeline **298 pytest + eval 3/3**, functions **226**, web **lint 0/0 + vitest 65/65 + build**, **RLS owner-isolation PASS**, **service-key bundle-leak PASS**, **axe 0 violations** on `/login`+`/dashboard`+`/runs`+run-detail+fixes (desktop+Pixel-7), **integrity-reviewer APPROVE**, **graph-keeper APPROVE** (writer). Migrations `0010` views + `0011` RLS write-lockdown + `0012` unique-constraint applied live. Pipeline Supabase writer merged to `master` + wired (`GOBLIN_SUPABASE_ENABLED=true`, client `promptgoblin.io`, owner atpatzat@gmail.com) — runs upsert as `approved=false` (human-gated). Live verified: promptgoblin.io 200, `/dashboard`→307 `/login` (SSR auth gate), www→301.
- **Dashboard seat split — READY TO APPLY** (2026-06-09). Add explicit `client_memberships` so one tenant can have multiple auth users without breaking RLS. Intended live seats for `promptgoblin.io`: `atpatzat333@gmail.com` = **admin** (approval UI + scan launches), `atpatzat@gmail.com` = **tier 3** scan-runner seat (launch scans from the dashboard). Google OAuth + custom auth sender remain Supabase project-config tasks, not app-code tasks.
- **Dogfood live scan reality check (2026-06-08):** the first live `promptgoblin.io` scan proved the dashboard/write plumbing, but the recommendations were not usable as AEO/SEO strategy yet. The run mislabeled Prompt Goblin as an AI prompt utility, produced prompt-generator queries, and surfaced bad competitors (`urbandictionary.com`, `define.php`). Do not act on that recommendation set. Immediate fix: make homepage facts authoritative in recon, clean query-expansion output, reject bogus competitor domains, then re-run before acting. See `docs/promptgoblin-dogfood-aeo-seo-plan.md`.
- **Wave 2 — vector-rag (#6):** deferred (owner).
- **Verification truth:** pipeline **298 pytest + eval 3/3** · functions **226** · web **65 vitest + e2e** (older 289 / 138 / 32 counts are stale).
- **Open owner items:** ~~session-pooler string (#4)~~ ✅ · ~~service-role key (#5)~~ ✅ set on DO + envs · ~~DO Static→Node + #5 cutover~~ ✅ LIVE · **🔑 STILL OPEN: rotate exposed secrets (Stripe `sk_live_` + DB password)** · embeddings key (if #6 resumes) · optional `app.` subdomain (not needed — dashboard lives on apex).

## Status

- ✅ **Marketing site live on `promptgoblin.io`** — canonical domain migrated from `promptgoblin.zatgeist.com` 2026-06-03 (commits `0de4b67`, `5ed0022`, `fdf9c3b`). `.io` is PRIMARY; `www` + `zatgeist` are 301-redirect aliases; Cloudflare DNS (grey/DNS-only) → DO app; Let's Encrypt certs ~Sep 2026. DO App Platform, deploy-on-push from `main` (origin: github PatZat7/promptgoblin). axe 100/100, 0 violations.
- ✅ **Pipeline task (c)** — done.
- ✅ **Pipeline task (a)** — per-discipline self-healing verify strands; eval gate green (seo + a11y `fires+converges@1`, `bounded@cap`), 186 tests pass. Committed.
- ✅ **Leads** — 27 sendable in `pipeline/sales/leads30.md`; prioritized, name-filled DMs in `pipeline/sales/dms_to_send.md`.
- ✅ **Agent team** — 8 specialists in `.claude/agents/` + this PLAN + project `CLAUDE.md`.
- ✅ **Multi-agent coordination board** — `COORDINATION.md` is now the visibility board for Codex / Claude / Hermes lanes, context preflight, status stamping, and merge gates. `PLAN.md` remains the execution contract; Codex integrates.
- ✅ **Feedback inbox** — Claude/Hermes review notes land in `feedback/claude/` and `feedback/hermes/`; Codex records integration decisions in `feedback/codex/` before updating `PLAN.md`, specs, migrations, or code.
- ⚠️ **Current verification snapshot (2026-06-06, Wave 1 branch)** — `pipeline .venv\Scripts\python.exe -m pytest -q` PASS (287 tests) and `pipeline .venv\Scripts\python.exe -m goblin.eval.run_eval` PASS (3/3 cases + global checks); `functions npm test` PASS; `web npm test` PASS (32 tests); `web npm run build` PASS and emits `/methodology` + `/learn/aeo-vs-geo`. `web npm run lint` is still red on baseline lint issues outside the Wave 1 docs/prune slice; do not claim lint green until separately fixed. Integrity-reviewer APPROVED; graph-keeper final APPROVE is still pending/unavailable after the final redirect-SSRF fix.

## In flight

- 🚧 **Next.js migration of the public site → `web/`** (2026-06-03, branch `style/pre-launch-polish`). Rebuilding the Babel-in-browser SPA (`app.jsx` 2123 / `styles.css` 1031, one file each) as **Next 16 App Router + React 19 + TS + Tailwind v4 + Zustand**, **static-export** (`output:'export'` → ships to DO App Platform as static, same as today). Goal (owner): public-site code well-organized + **design system locked** before the customer dashboard. **Foundation DONE + verified** (`next build` green; browser-verified at :3010 — RSC server-render, client-island hydration, animation, theme toggle + Zustand persistence + no-flash `data-palette`, palette-reactive `@theme inline` tokens dark↔bone, `next/font`, 0 console errors, 0 mobile overflow). Locked pattern = **RSC server shell + minimal `'use client'` island + co-located `*.module.css` with semantic names + `var(--token)`**; reference impl = `web/components/sections/GoblinMesh/`. Conventions in **`web/AGENTS.md`** (read by Claude AND Codex; honest-broker honored). The **live site is untouched** (DO still builds `/` root `index.html`; `web/` doesn't deploy yet). **Batch 1 DONE + verified** (2026-06-03): **Hero · Services · Pricing** ported from the current `app.jsx`/`styles.css` (NOT the handoff — owner confirmed only the Goblin Mesh came from the handoff). RSC server shells + minimal islands (`GoblinArt` blink, `ServiceAccordion` keyboard-accessible `role=button`+`aria-expanded`), reusable `Reveal` + global `.btn`, `next build` green + browser-verified desktop/mobile (0 console errors, 0 h-overflow, real Stripe links). **Batch 2 DONE + verified** (2026-06-03): Spellbook · Stats · Marquee · IndexNow + `Sprite` primitive + `Reveal`/`line-mask` shared bits (build green, desktop/mobile, 0 console errors). Page order now Hero→Spellbook→Mesh→Stats→Marquee→IndexNow→Services→Pricing. **Batch 3 DONE + verified** (2026-06-03): HowItWorks engine-diagram (animated packet, eval-gate pass/fail) + LiveScan terminal (full state machine idle/sample→scanning→results/error; scan `fetch`→DO Functions wired via `lib/scan-api.ts`; **honest-error path verified on localhost** — CORS-fail → error card, NO demo fallback; real results only round-trip on deployed origin). Shared: `lib/analytics.ts` (PostHog shim, no-ops till wired) + `lib/scan-api.ts`. Page order: Hero→Spellbook→HowItWorks→Mesh→LiveScan→Stats→Marquee→IndexNow→Services→Pricing (0 console errors, desktop/mobile). **Batch 4 DONE + verified** (2026-06-03): Contact/Summon (Web3Forms lead form via `lib/analytics.ts` `submitWeb3Form`; honeypot; success/error states; server shell + `SummonForm` client island) + contact side-rail + colophon. **ALL 11 page sections now ported** (Hero · Spellbook · HowItWorks · Mesh · LiveScan · Stats · Marquee · IndexNow · Services · Pricing · Contact), build green, 0 console errors, desktop + mobile. **NEXT (finishing pass):** ✅ custom pixel cursor (ported from the `promptgoblinv1` Claude Design `PixelCursor` — lime square, lerp-follow, terminal labels; refined so per-section ambient `data-cursor` labels show without the enlarge, controls get action labels; palette-reactive, hidden on touch + reduced-motion; native cursor hidden only on fine pointer; 41 labels wired); **FINISHING PASS DONE + verified** (2026-06-03): Loader + PostHog (init moved to a post-hydration `useEffect` — pre-hydration `instrumentation-client` init caused a React-19 script-hoist hydration mismatch; `phc_` key + `?ph_optout=1` owner opt-out) + static assets (`app/icon.svg`, `app/apple-icon.png`, `public/og-image.png`, `public/llms.txt`, `app/robots.ts`, `app/sitemap.ts` — `force-static`) + `<head>`/viewport (theme-color, canonical, 6 JSON-LD incl. BreadcrumbList restored). **`qa` axe-core gate: PASS — 100/100, 0 violations across all 8 states** (dark ×4 vp, bone ×2, services-expanded, scan-error). Fixed latent **bone-palette** contrast the original carried: `--muted`→0.66, `--faint`→0.60, palette-reactive `--fire`/`--fire-2`, deepened bone `--lime`→#3f6212 (the #4d7c0f used as TEXT was only 4.45:1), de-tinted the scan error card. **NEXT (owner-gated): deploy cutover** — point DO App Platform at `web/` (build → static `out/`) instead of root `index.html`; owner reviews before anything ships. Old `app.jsx`/`styles.css` stay as the source-of-truth reference until parity. _Dashboard (Next + Supabase) remains a separate later workstream._
- 🎨 **Style pass before next live push** (2026-06-03) — owner wants style changes before merging to `main`. Uncommitted local edits present: `styles.css` (in-progress `.how-grid` tweak — dropped `grid-template-columns: 1fr 1.4fr` + `align-items`, kept `gap: 48px`). Untracked: `PRODUCT.md` (new product brief), `.agents/`, `.claude/skills/`, `skills-lock.json` (skill installs). **Do this work on a branch; do not merge to `main` until approved.**
- 🎨 **Site redesign (2 directions) + client dashboard** — planning done 2026-06-02. Deliverables in repo root: `REDESIGN_BRIEF.md` (mobile-first prompt for Claude Design — 2 directions A/B + login/dashboard), `COPY_V2.md` (plain-language copy, integrity-gated), `DESIGN_PLAN.md` (stack call + tooling + phases), `AEO_VS_COMPETITORS.md` (measure-vs-competitors guide). Next: owner hands `REDESIGN_BRIEF.md` to Claude Design → 2 prototypes → evaluate → build → `qa` gate → ship. **Sign-offs:** (1) public-sector wording (USDA/Chicago/TX/IL — exact substantiable phrasing) — _hard blocker before any prospect sees §05/bio_; (2) dashboard stack go-ahead (Next.js + Supabase, separate `app.` subdomain); (3) optional direction lean. **All RESOLVED 2026-06-02:** gov wording owner-confirmed; dashboard stack = **Next.js + Supabase**; **direction:** built Direction A (Refined Terminal) on branch `feat/redesign-direction-a` (`8261bd2` — axe-clean both themes, integrity-approved) — but on review **the owner preferred the ORIGINAL style; reverted to it (2026-06-02).** The live site was never changed (the redesign was never merged or deployed). Direction A is **preserved on its branch** for reference. **RESOLVED + SHIPPED 2026-06-02:** built a **hybrid** on the original style — ported the "how we actually move the needle" engine-motion diagram + the Visibility Mesh, removed the Work/case-study section, airier spacing — then **rebuilt the free scan** (real backend-driven multistep + visual stepper + measured results card) and swept review copy to **software-engineer-reviewed**. Merged `feat/hybrid-move-needle` → `main` (deploy-on-push). Dashboard MVP (Next.js + Supabase) still queued.
- 📊 **Pipeline task (b)** — ✅ **v1 complete, green, graph-keeper APPROVED, committed (`183c980`) + merged to pipeline `master`** 2026-06-02 (local; pipeline repo is ahead of origin by 6 — not pushed). See Recently done.

## Recently done

- ✅ **Scan regressions swept + locked** (2026-06-06, Claude-integrator):
  - **jQuery restored** — commit `1573276` had filtered jQuery out of the detected stack ("jQuery means plain HTML"), so WordPress+jQuery sites (deadmau5.com) reported only "WordPress" and lost real implementation signal. Reverted just that filter, kept `1573276`'s good half (CSS frameworks always reported + Foundation/Materialize checks), flipped the tests it added, and added a deadmau5-style WordPress+jQuery regression lock (`a0b2297`). **DEPLOYED + live-verified** (`doctl serverless deploy`, 2026-06-06): a live tier1 probe of deadmau5.com now returns `WordPress` (high) + `jQuery` (low).
  - **Scan stepper verified INTACT (not lost)** — owner thought the per-phase visual stepper was dropped in the `web/` cutover. Traced it: `web/components/sections/LiveScan/ScanStepper.tsx` is a faithful, complete port of the old `app.jsx` stepper (`46d786f`) — same pending→active→done logic + progress bar + `%` counter, rendered in BOTH the scanning view and the results view (the ✓ rows in a result card ARE the stepper in done-state). No code change needed. Owner confirmed "leave it."
  - **Regression test suite** — new `functions/test/scan-regression.test.js` (77 offline checks, zero keys/network) locks expected scan results for a URL-category matrix (waf · spa · service/gov · clean · wordpress · shopify) so this class of capability regression is caught on every commit; plus an opt-in live-URL smoke list (`functions/test/live-urls.json` + `npm run smoke`) that is **not** CI-gated (network-flaky). functions test now **226 checks** green (`477b7e1`).
- ✅ **CI lint unblocked** — cleared 7 pre-existing ESLint errors that had `npm run lint` red on `main` (jsx-no-comment-textnodes ×3, react-hooks/set-state-in-effect ×4, unused-var ×1); behavior-neutral (`1ee611e`).
- ✅ **Stack-specific fixes + AI-ready prompt + auto-recon (the "customized report" upgrade)** (2026-06-05). Three linked capabilities so a scan ships a *customized* solution, not a generic checklist — all green, all gated:
  - **`stack_detect` node** (new; runs after `seo_audit`) — regex-fingerprints the client's stack (WordPress · Next.js · Shopify · Webflow · Wix · Squarespace · Framer · Ghost · Gatsby · Hugo · Drupal · Joomla) from the cached homepage HTML; honest `platform="html"`/low-confidence fallback when nothing matches or HTML is absent.
  - **Stack-aware `recommend._snippet_for`** — schema/SEO/a11y fixes now emit **platform-specific code** (Next.js `metadata` + `JsonLd`; WordPress `functions.php`/Yoast; Shopify Liquid; Webflow/Squarespace settings-panel paths) with a generic-HTML fallback. Hygiene disclaimers retained ("NOT a proven citation lever"; a11y "static pre-screen ~57%").
  - **AI-ready prompt artifact** — `ship_pr` writes a **third deliverable**, `out/goblin-<slug>-<stamp>-ai-prompt.txt`: a drop-in prompt the client pastes into their own ChatGPT/Claude that walks them fix-by-fix (explain → apply for their stack → confirm) and folds in the community/mention items. `report_paths` gained an `ai_prompt` key. Honest close: "no citation number guaranteed — we measure the delta."
  - **`recon` node** (new; **runs FIRST**, before `listen`) — the "research the company for them" agent. Infers the **category/topic + one-line summary** (from homepage title/meta/h1 offline, or a live Perplexity/OpenAI call when keyed); **auto-identifies up to 2 competitors** (ONLY when the operator passes none — explicit competitors always win); runs an **early tech-stack scan** and caches the single homepage fetch for `seo_audit`/`a11y_audit`/`stack_detect` to reuse. Honest-broker: auto-competitors are a flagged **inference** (carry confidence + source + a "verify" note), never asserted fact; the mock pair is illustrative and **pinned to the `MockClient` fixtures by a test**. New state key `company_profile`; graph `START → recon`; CLI prints a recon summary + `--competitors` now documents auto-discovery.
  - **Free-scan form simplified to just `yourbrand.com`** (+ email, which stays — it's the lead + results delivery). Removed the optional **competitor** and **tech-stack** inputs (recon + the backend HTML fingerprint handle both now); reworked the now-stale disclaimer + Tier-2/stack copy. _The Tier-2 live-fetch branch is kept but currently dead — a deliberate hook for later feeding it a recon-discovered competitor; flagged for cleanup if not wired._
  - **Pricing → monthly, cancel-anytime** across all tiers, then **dropped to an aggressive land-grab ladder: Scout $997/mo · Warband $3,500/mo · Warlord $9,500/mo** (owner call — new to market, prioritizing first-client conversion + case studies over margin; intentionally *below* the research band of $1,500–$1,997 for Scout, raise once there are logos). Updated everywhere prices live: `pricing.data.ts`, `Pricing.tsx` note ("monthly · cancel anytime · no sales call"), `lib/site.ts` TIERS (feeds Offer/OfferCatalog JSON-LD), `lib/faq.ts`, `web/public/llms.txt` + root `llms.txt`, and both copywriter agent definitions. ✅ **Stripe Payment Links regenerated** (2026-06-05, via Stripe API, LIVE mode): new monthly links at $997/$3,500/$9,500 (Scout flipped one-time→recurring), `STRIPE_LINKS` rewired, and **all 6 old live links deactivated** so no path sells the old price.
  - **Verification:** **211 pytest pass** (206 + 5 new `test_recon.py`); **eval gate PASS** (3/3, mean 1.000) incl. the `no_competitors` case now exercising recon auto-discovery; site **`npm run build` green** (0 TS errors). Reviewed via a **parallel review workflow**: `graph-keeper` **APPROVE**, `integrity-reviewer` REVISE→**fixed** (3 copy hedges: "citation-acquisition"→"citation-earning"; softened "auto-identified competitors" to an inference the client confirms), `qa` **PASS**, completeness-critic REVISE→**fixed** (README node order + architecture diagram; `site.ts`/`faq.ts` "one-time"→"monthly"; `hygiene.js` ×2 stale "enter your CMS" note). **Nothing committed/deployed yet** (style-first deploy rule); `functions/` needs a redeploy for the hygiene.js copy change to go live.
- ✅ **Canonical domain migrated to `promptgoblin.io`** (2026-06-03). Bought from Cloudflare Registrar 2026-06-02. DNS stays on Cloudflare (Registrar forbids 3rd-party NS) — grey/DNS-only CNAMEs (`@` + `www`) → `promptgoblin-opz88.ondigitalocean.app` (orange-proxy would 24h-stale the deploy-on-push site via DO `s-maxage=86400`). DO app domains: `.io` PRIMARY, `www` + `zatgeist` ALIAS, all ACTIVE w/ LE certs. DO ingress 301s: `www`→apex, `zatgeist`→apex (App Platform doesn't auto-redirect aliases). Repo canonical swap `0de4b67` (index.html canonical/OG/Twitter/JSON-LD + sitemap + robots + og-card), 0 leftover zatgeist refs. Functions CORS `5ed0022` → `https://promptgoblin.io` (deployed via doctl, verified). Site WIP `fdf9c3b`: removed unbuilt Quotes/Scrolls placeholder sections + nav + CSS, "summon another" reset, clear-email-on-reset, mobile HUD toggle fix, how-it-works spacing. **OPEN (user-side):** email DNS at Cloudflare — Zoho free plan for `goblins@promptgoblin.com` (MX mx/mx2/mx3.zoho.com; SPF; DKIM `zmail._domainkey`; DMARC `_dmarc` p=none).
- ✅ **Backend honest-broker fix — "Missing Product schema" false-flag resolved** (2026-06-02). Tier-1 scan + pipeline now treat **Product as commerce-only**: flagged only on commerce signals (`Offer`/`AggregateOffer`/`Product` schema, `og:type=product`, or price markup) and **NEVER** for service/government sites (which correctly use Service/Offer/OfferCatalog). `functions/lib/hygiene.js` fixed → 40/40 unit tests → synced tier1 mirror → **deployed** (`doctl serverless deploy --remote-build`) → **live-verified** (usa.gov + promptgoblin.zatgeist.com show no Product finding; a commerce page is still flagged at MED). Pipeline **mirrored by graph-keeper**: a shared `missing_schema_types()` now drives both the `schema_audit` node + `outreach.py` (also fixed the dead `expected_types_for` wire), Product demoted to MED (sev 3) → **205 pytest pass + eval gate PASS (3/3, mean 1.000)** → committed `bfeef6e` (pipeline repo, local). Site commit `c381606` pushed → `main`. **The free scan is now safe to pitch to gov/service prospects.**
- ✅ **Hybrid homepage + free-scan rebuild — SHIPPED** (2026-06-02, `feat/hybrid-move-needle` → `main`, deploy-on-push). Kept the original style; ported the "how we actually move the needle" engine-motion diagram + Visibility Mesh; removed the Work/case-study section; airier spacing; dropped the Spellbook title bolt. **Free scan rebuilt:** honest **backend-driven multistep** (each phase resolves to a MEASURED Tier-1 value — KB, robots/llms presence, JSON-LD type count, hygiene score), a **visualized stepper** beside the terminal (pending→active→done), a **results card** (score band + schema chips + disclaimer + CTA), honest failure path (no demo fallback on a real submit), StrictMode-safe run token (**killed the duplicate-line bug**). Idle terminal = clearly-labelled SAMPLE. Copy swept **"human-reviewed" → "software-engineer-reviewed"** site-wide (kept "usable by humans" end-user taglines). Custom **cursor disabled on touch/coarse-pointer** devices. **integrity-reviewer APPROVED** (scan copy + SWE sweep); **design-system** polished the visuals. Verified **0 axe contrast + 0 console errors** (idle/results, desktop/mobile). _Note: real Tier-1 results round-trip only on the live origin (CORS); localhost shows the honest error card — verified the result path via a stubbed report._
- ✅ **Pipeline task (b) v1 — PostHog recs engine + LangGraph outreach workflow** (2026-06-02; graph-keeper **APPROVED** both design + implementation). NEW: `goblin/posthog_client.py` (mock-first reader — live needs `POSTHOG_PERSONAL_API_KEY`+project, else honest-degrade `posthog_live=False`; PII-free events), `outreach_state.py`, `outreach_nodes.py` (`blend_rank` + 5 nodes wrapping the tested `outreach.py`), `outreach_graph.py` (standalone StateGraph: ingest→score→draft→[interrupt: human_review]→finalize; own fallback runner; **never sends**), the `outreach-graph` CLI verb, and a SEPARATE eval registry (`eval/outreach_cases.py` + `outreach_metrics.py`) surfaced as a new **`outreach honesty (task b)`** global check. **Audit spine + `GOLDEN_CASES`/`score_state` byte-for-byte untouched.** Honest-broker enforced in code + eval: never-sends (no transport; status only `{draft, approved_ready_to_send}`), honest-degrade (signal weight→0 when not live ⇒ pure-hygiene ranking), CAN-SPAM/CASL refusal, unreadable≠scored, PII-free signal. **Gate: 201 pytest pass (1 skip) + eval PASS** (3/3 cases + all 3 global checks). Docs: `.env.example` (`POSTHOG_*` + blend weights) + README `outreach-graph` section. _Uncommitted in the pipeline repo pending owner OK._
- ✅ **Backend verified** (2026-06-02): `functions/` Tier-2 hardening finished — Perplexity client now has a 25s AbortController timeout, a `max_tokens` cap (512), and a robust `extractCitations` (top-level + `search_results` + nested `message.citations`, with dedupe). **36/36 unit tests pass** (zero keys/network). Live probes: **Tier-1** OK + honest disclaimer intact; **Tier-2 live & `configured:true`** — the `PERPLEXITY_API_KEY` is already set on the deployed action (it is NOT dormant; it ran a real `sonar` query and returned real citations); **PostHog ingestion confirmed** (`status: Ok` — one labeled `backend_verify_probe` / `pg-backend-verify-bot` event, safe to delete). Added cross-platform `functions/build-sync.js` (the bash `build.sh` can't run on Windows; DO's remote deploy still uses build.sh). **Committed `5bc1443` → merged to `main` + pushed → deployed (`doctl serverless deploy functions --remote-build`) and live-verified post-deploy** (Tier-1 OK; Tier-2 `configured:true`, 2 results).
- ✅ **Redesign planning + pipeline verification** (2026-06-02): produced `REDESIGN_BRIEF.md` (keep logo/cursor/terminal-dark-fantasy + "get found by robots, stay usable by humans"; minimal/clean; 2 directions; unique diagrams for citability + the engine; gov proof; login+dashboard; mobile-first; AA), `COPY_V2.md` (copywriter → integrity-reviewer **REVISE → fixed**: "three pipelines" → one pipeline w/ bounded self-healing loops; softened unverified dogfood fix-counts; dashboard sample-data markers; gov wording conservatively flagged), `DESIGN_PLAN.md`, `AEO_VS_COMPETITORS.md`. Tooling researched: **`impeccable`** Claude Code design skill (verified real — `/plugin marketplace add pbakaus/impeccable`), Figma/Playwright/a11y/Supabase/GitHub MCP, GSAP+ScrollTrigger/Lenis/Motion/Three.js. **Honest stack call:** awwwards site can stay near-zero-build; login+dashboard needs a real backend (Next.js + Supabase). **Pipelines verified GREEN:** 186 pytest pass (1 key-gated skip) + eval gate PASS (3/3 cases, mean 1.000; LOOP1 fires@0/converges@1/bounded@cap; seo+a11y verify strands converge & bounded).
- ✅ **Pre-launch QA + ship** (2026-06-02): `qa` ran axe-core across 1440/768/375/360 → **0 violations**. One blocker found (Scrolls "soon" cards at 4.3:1) and fixed by `design-system` — dropped the `opacity:0.55` card dim for a dashed muted border so text stays full-contrast (~10.5:1, re-verified). **Favicon** redone per request: transparent (no box), theme-adaptive via `prefers-color-scheme` (white on dark / black on light browser UI) — both schemes verified. `fix/mobile-and-copy` (7 commits) merged to `main` + pushed → deploy-on-push.
- ✅ **Site polish batch** (2026-06-02 — copywriter + design-system → integrity-reviewer **APPROVE** → preview-verified at :8127, 0 console errors):
  - Mesh graph copy fixed: now 5 answer engines (was 3), `12 gaps · 4 a11y`, `you vs. 6 competitors` (was the cryptic `graph(82 edges)`), + a `sample run` illustrative marker (same misread risk we closed on the scan demo).
  - Hero sub-line A applied (DM message-match); footer social links → non-clickable `soon` spans.
  - STATS de-risked: dropped unproven `100% schema valid` + `180ms TTFB`; now `5 engines · 6 JSON-LD blocks · 7 nodes · 0 auto-deployed` (all provable).
  - Share assets ready: `og-image.png` (1200×630, real goblin logo) + `og:image`/`twitter:image` + sharper og:description; **SVG favicon** (real logo) wired (`favicon.svg`, source `og-card.html`). Both serve 200.
- ✅ **Site copy refresh** (2026-06-02, `copywriter` → `integrity-reviewer` **APPROVE** → main-thread render-verify, 0 console errors). Applied to `app.jsx` (+ tiny `.scard-soon` rule in `styles.css`): (1) Live Scan demo neutralized — `n=2,481 sources`→`[illustrative]`, `4 mentions · avg position 2.3`→`competitor cited · you're not — [sample output]`, issue block reordered so the citation gap is HIGH/first and schema demotes to MED (now matches "schema is hygiene"); (2) dead Scrolls `href="#"` cards → non-clickable `field note · soon`; (3) capacity unified (`taking 3 clients · Q3–Q4 26` + `3 slots · Q3–Q4 2026`); (4) `30+ tasks`→`scored by impact × effort`. Analytics diff preserved.
- ✅ **Mobile UI fix** (2026-06-02, `design-system` + main-thread verify) — CSS-only, 3 bugs closed, verified in `:8127` preview at 360/375/768px (0 doc overflow, 0 clipped lines):
  1. Work-grid clip — `.wcard{min-width:0}` lets the `1fr` track shrink (408→371px); `.wterm .ln` wraps @≤700px so the dogfood proof lines ("…not a promise", "…2 content") aren't ellipsis-truncated on phones (main-thread refinement after preview check — agent's `overflow-x:auto` couldn't engage, `.ln` clips itself at `styles.css:386`).
  2. Services sliver — span the class-less lead/desc wrapper @≤760px (`styles.css:429`); `.lead` 40→341px.
  3. Footer touch targets — `github`/`x.com`/`substack` 18→28px, `hi@` 40px (clears WCAG 2.5.8) @≤600px.
  - Kept the prior good work (HUD trim @600px, mesh flatten @700px). `app.jsx` untouched.

## Queued

- 🆕 **Auto Tier-2 scan on account creation** (owner request 2026-06-08) — when a new account is provisioned (auth user + `clients` row), automatically kick off a **Tier-2 live scan** for that client's domain and land the results in the dashboard.
  - **Trigger options (decide):** (a) Supabase DB trigger / webhook on `clients` INSERT → calls a server endpoint; (b) the account-provisioning flow (today: manual via service role — until self-signup exists) calls it inline; (c) Supabase Auth Hook post-signup. Lean (b) now (manual provisioning), design for (a)/(c) when self-signup lands.
  - **What runs:** Tier-2 is the email-gated live engine (Perplexity) — clarify whether "Tier-2 scan" means the **DO `functions/scan/tier2`** action or a **full LangGraph pipeline run** (`goblin`, which already writes `runs`/`recommendations`/`verification_results` to Supabase via `supabase_writer`). For dashboard data, the **full pipeline run is the right path** (it populates the scorecard); the Tier-2 function alone only returns citations. Recommend: provision → enqueue a full pipeline run for the domain, writer attributes it to the new `client_id`/`owner_user_id`.
  - **Honest-broker / cost:** Tier-2 + a live pipeline run **cost real API calls** (Perplexity/OpenAI) — must be **rate-limited + abuse-guarded** before self-signup is ever enabled (auto-scanning on every signup is a spend/abuse vector). Results land **`approved=false`** (human-gated; not client-visible until reviewed). Never auto-approve. A blocked/WAF/unreadable domain is flagged, never scored 0.
  - **Async:** a scan takes minutes — do NOT block account creation on it. Enqueue (job/queue or background) + show the dashboard's "run in progress / no run yet" state until it lands.
  - **Gate:** `graph-keeper` (touches the pipeline writer path), `integrity-reviewer` (any client-facing "we're scanning you" copy).
- ✅ **Scout price — DECIDED + WIRED** (2026-06-05): aggressive land-grab — **$997/mo** (Warband $3,500, Warlord $9,500). Below the research band ($1,500–$1,997 for founders) on purpose: win first clients + case studies now, raise later. Stripe links **regenerated (live, monthly) + old links deactivated** — checkout matches the displayed prices.
- 🚨 **Fix current pipeline pytest blocker** — update either `_fetch_jsonld_types`/schema-audit fetch-note wording or the test expectation so the network-error path has a stable honest contract. Required verification: from `pipeline/`, run `.venv\Scripts\python.exe -m pytest -q` and `.venv\Scripts\python.exe -m goblin.eval`; both must pass before claiming pipeline green.
- ✅ **Redeploy `functions/`** — DONE 2026-06-08 (`doctl serverless deploy . --remote-build`): tier1+tier2 shipped, Scrapfly key preserved, 226 checks green, live tier1 probe confirms the "we auto-detect your stack" copy is live.
- 🧩 **Optional follow-up:** prune (or wire) the now-dead Tier-2 live-fetch branch in `LiveScan.tsx` — kept as a hook for feeding it a recon-discovered competitor; remove the two unused state slots if that wiring isn't planned.
- 🎨 **Build the chosen redesign** (after Claude Design returns A/B): implement mobile-first → `design-system` + `impeccable` polish → `qa` gate (axe 100/100, e2e) → ship via deploy-on-push.

- 🧭 **Immediate dogfood AEO/SEO queue (2026-06-08):**
  - **Fix recommendation targeting before acting:** recon/listen must keep `promptgoblin.io` in the AEO/GEO + technical SEO + accessibility lane, not the generic prompt-generator lane. Regression lock: Prompt Goblin homepage topic beats stale/guessed live research; malformed LLM preambles are not accepted as buyer queries; bogus competitor domains are rejected.
  - **Rerun the live scan after the recon/query fix:** accept recommendations only if they improve one of the real levers: brand mentions, Bing/web rank, crawlable answer-shaped content, accessibility/technical crawl hygiene. A low citation share should prevent a "100 score" framing.
  - **Bing Webmaster Tools guide:** `docs/bing-webmaster-tools-submission-guide.md` is the internal guide. Public/client-facing route is still queued (`/docs/bing-webmaster-tools`) and must keep the "submission is not indexing/citation guarantee" wording.
  - **Own-site content targets:** use `docs/promptgoblin-dogfood-aeo-seo-plan.md` as the search-term/content map. Priority pages before reels/social: how to show up in ChatGPT, Bing rank and AI citations, technical SEO for AI search, accessibility SEO audit, benchmark.
  - **Marketing asset generation pipeline:** backlog after Bing/content/recon are stable. Scope: research where to post, what to post, generate text drafts, then later generate designed assets. Needs a design system before visual automation.
- **Term target map for promptgoblin.io:** canonical list of discovery terms we want to rank/cite for, with categorized labels and expansion rows per Google fan-out behavior. See `docs/promptgoblin-aeo-seo-term-map.md`.
  - **Dashboard/product backlog from owner:** approval UI, "run your own scan" button on dashboard, check emailed scan styling, verify login/account creation end to end, verify dashboard report with code snippets/fixes, and prove the full report flow works.
  - **Public-site CTA backlog:** replace redirect-style summon flow with an in-page transition that reveals email capture while prioritizing pricing cards and the primary paid CTA.
  - **Competitor research backlog:** add a real web-search research agent for direct competitors. Example acceptance case: `walgreens.com` should surface pharmacies such as CVS/Rite Aid/Walmart Pharmacy, not generic health/reference domains.
  - **FAQ:** dedicated `/faq` route now exists; keep it single-sourced from `web/lib/faq.ts` and FAQPage JSON-LD.

- 🧪 **CI/CD eval gate wiring** — make the merge/deploy gate explicit before dashboard work starts:
  - **Functions:** `cd functions && npm test` must pass (zero-key, zero-network unit suite).
  - **Web:** `cd web && npm test && npm run build` must pass. Add browser/axe verification when public UI changes.
  - **Pipeline:** `cd pipeline && .venv\\Scripts\\python.exe -m pytest -q && .venv\\Scripts\\python.exe -m goblin.eval` must pass. Optional full harness: `.venv\\Scripts\\python.exe harness/run_harness.py`.
  - **Blocking rule:** no merge to deploy-on-push `main`, no functions redeploy, and no "pipeline green" claim if any required gate is red. Mock/skipped/demo paths are reported as such.
  - **Visual regression (Playwright MCP):** any public-site or dashboard UI change must include before/after screenshots captured via Playwright MCP against key viewports (desktop 1440, tablet 768, mobile 375). Screenshots go to `web/tests/visual/` with a stable naming scheme and are reviewed before merge. No console errors, no new axe violations.

- 📊 **Client dashboard MVP** — Next.js 16 App Router + Supabase on `app.promptgoblin.io` subdomain. **Full spec:**
  - **Stack:** Next.js (same `web/` monorepo, separate `app/` subdomain target) + Supabase (postgres + pgvector + auth). Supabase schema from vault `Prompt Goblin - Supabase + Vector DB Plan.md` — create committed migrations before any API routes.
  - **Migrations + local proof:** add a `supabase/migrations/` workstream (or the chosen equivalent if the monorepo structure changes), including `clients`, `competitors`, `runs`, `citations`, `competitor_citations`, `crawl_pages`, `page_chunks`, `recommendations`, `verification_results`, RLS policies, and seed/sample fixtures. Verify locally with Supabase CLI before wiring UI.
  - **Client-visible run history** — `/runs` page lists every scan run for the client's domain(s): timestamp, status badge, visibility score delta vs prior run.
  - **Rescan snapshots** — each run row is a full snapshot (stored as `graph_snapshot jsonb` + individual score columns in `runs` table); drill-in to any historical run; no data lost on rescan.
  - **Citation scorecards** — per-run view: visibility score, citation gap count, you-vs-competitor citation diff, platform breakdown (ChatGPT vs Google AIO), verified/unverifiable/fabricated annotation per citation (once verification layer ships).
  - **Fix queue** — `recommendations` table surfaced as a prioritized queue (HIGH → MED → LOW); each item shows kind, stack-specific snippet, rationale, and current `status` (pending / approved / implemented / verified).
  - **Eval status** — live badge per run from the `verification_results` table: eval gate PASS/FAIL, which checks passed, which regressed.
  - **Human approval state** — fix-queue items that require human review are gated: `human_reviewed: false` items are locked (greyed, "awaiting review" badge); `approved: true` unlocks the implementation snippet. No fix ever ships without this flag.
  - **Server-side secrets only** — Supabase service-role key, Stripe webhook secret, and any scan API keys live in Next.js server environment only (`NEXT_PUBLIC_*` is forbidden for secrets); client-side code only touches the Supabase anon key scoped to the authenticated user's rows (RLS enforced).
  - **Sample data clearly marked** — until a real run exists for the authenticated client, every metric shows a `[sample]` chip and a `run the scan to see real data` CTA; no sample number is ever styled to look like a real result.
  - **Scan visual proof / Playwright proof artifacts** — every client-visible run must retain visual evidence the scan rendered correctly: before/after screenshots of the scan terminal + results card on desktop/tablet/mobile, stored as run artifacts and surfaced in the run detail view. Captured via Playwright/Browser MCP; serves as regression proof plus proof that the backend-driven scan actually produced visible output for that run.
  - **Automated dashboard screenshot gate** — before any dashboard release, run Playwright MCP against `/dashboard`, `/runs`, `/login`, and the free-scan flow; keep baseline diff artifacts and require human review for any visual change. Same axe + console-error standard as public-site QA.
  - **Login / auth:** Supabase Auth — dedicated `/login` page on `app.promptgoblin.io` (magic-link email + optional Google OAuth). Unauthenticated requests to any dashboard route redirect to `/login`. Session managed server-side via Supabase SSR cookies (no client-side token exposure). Post-login lands on `/dashboard`. Dashboard access is now split into **client memberships**: owners/admins and member seats are attached to the same `client_id`, and RLS allows reads by `owner_user_id` **or** an explicit membership row. Current intended seats for `promptgoblin.io`: `atpatzat333@gmail.com` = admin / approval UI, `atpatzat@gmail.com` = tier 3 scan-runner. Account provisioning is manual for now; self-signup is off until there's a billing gate.
  - **Prerequisite:** deploy the Next.js `web/` public-site cutover first; then provision Supabase project and run migrations; then scaffold the `app/` subdomain.
- 🧱 **Vector/RAG ingestion workstream** — make pgvector useful, not just present:
  - **Acquire:** store crawl attempts in `crawl_pages` with source type (`static_http`, `browser_render`, `sitemap_export`, `search_console_export`, `bing_export`, `client_upload`, `cms_api`) and honest status (`ok`, `blocked`, `unreachable`, `manual_needed`).
  - **Normalize:** extract title/meta/headings/body/JSON-LD from each acquired page; preserve source URL, fetch timestamp, HTTP status, and acquisition method.
  - **Chunk/embed:** chunk normalized content into `page_chunks`, embed with a fixed model/dimension matching the `vector(1536)` plan, and upsert by content hash so rescans don't duplicate unchanged chunks.
  - **Retrieve:** during recommendations, retrieve relevant client/competitor/source chunks by query/gap; include provenance in report JSON and AI-prompt artifact.
  - **Refresh cadence:** expire stale chunks by segment (finance faster, evergreen docs slower); never use stale content without labeling it as cached.
- 🛡️ **Protected-site / WAF fallback ladder** — turn the Meijer-style block into a paid-audit path without evasion language:
  - Tier 1 free scan remains honest: blocked WAF/CDN/JS-heavy sites get **no hygiene score**, plus a note that the quick static fetch could not read the page.
  - Paid Scout acquisition order: static HTTP fetch → browser-rendered capture (Playwright/Browser MCP, normal browser session, no bypass promises) → sitemap/Search Console/Bing export → client-uploaded HTML/docs → CMS/API pull with client consent → mark `manual_needed`.
  - Every acquisition attempt writes method/status/evidence to `crawl_pages`; reports say exactly which method produced the content.
  - Do **not** promise WAF bypass, captcha solving, or access-control evasion. Position this as consented alternate acquisition for protected sites.
- 📱 **Social-presence agent flow** — stand up real github / x.com / substack profiles, seed content, then wire the real URLs into the footer (`app.jsx` ~1193, currently non-clickable "soon" spans). Until then the footer honestly shows them as forthcoming.
- _(Pipeline task (b) → now **In flight**, above. `functions/` Tier-2 hardening → **done**, see Recently done.)_
- 📧 **Email DNS** — _user only, at Cloudflare_ — Zoho Mail free plan for `goblins@promptgoblin.com`: MX (mx/mx2/mx3.zoho.com 10/20/50), SPF (`v=spf1 include:zohomail.com ~all`), DKIM (`zmail._domainkey`), DMARC (`_dmarc` start `p=none`). Last remaining migration piece.
- 📨 **Send DMs** — _user only_ (I draft, never send). Start with Matt Aitken / Trigger.dev. Send 5–8, not 27.
- 🔑 **HIGH-RISK: rotate secrets** — _user only, but blocking for serious ops_ — exposed DigitalOcean token + WORKDAY_PASSWORD. Rotate before new deploy automation, CI secret wiring, dashboard service-role setup, or broader repo sharing. Never paste replacement values into chat or committed files; keep in gitignored `.env` / platform secret stores only.
- ⬆️ **Push pipeline repo** — local pipeline `master` is ahead of origin by ~6 commits (task (b) + Product-schema fix), not pushed.
- 🧹 Optional: drop the throwaway `browser_audit/_contrast_recheck.json` artifact.

## Research-driven backlog (2026-06-06 vault research — 5 files)

> Source: `Prompt Goblin - AEO GEO Industry Deep Research 2026`, `Citation Market Research Compilation 2026`, `Citation Tools And Integrity Research 2026`, `Reddit Community Signal 2026`, `Supabase + Vector DB Plan`. See full analysis in vault.

- 📋 **Documentation plan** — `DOCS_PLAN.md` now exists in the repo root (2026-06-06). Covers all four doc audiences: prospect-facing methodology + methodology explainers, client-facing onboarding + report guides, public-authority content (benchmark report + AEO/GEO explainers), and internal pipeline/agent docs. Work it in parallel with the dashboard build — docs unlock sales conversations before dashboards exist.

- 🔍 **Citation verification layer** (`pipeline/` + `functions`) — The fabrication crisis is the sharpest differentiator (51% hallucination rate in independent investigations; 12x increase 2023→2026; 206+ court sanctions). Competitors monitor/audit; almost none *verify*. Design: add `verify_citations` after retrieval/Tier-2 citation capture. Cross-check each cited URL with HTTP HEAD/GET, canonical URL normalization, content/snippet match, and optional indexed-source evidence; annotate each citation as `verified` / `unverifiable` / `fabricated`. Fabricated/unverifiable citations must lower confidence and appear in the dashboard + AI-prompt artifact. **Requires:** `graph-keeper` review; add citation status enum/fields in Supabase; tests for verified, canonical-redirect, blocked-but-unverifiable, and fabricated citation cases; eval honesty invariant that no unverified citation is rendered as proven.

- 📊 **Topical authority proxy score** (`pipeline/goblin/nodes/seo_audit.py`) — The #1 citation predictor (r=0.41) is unmeasured. DA/backlinks (what we currently fingerprint) explain <4% of variance. Proxy: count connected named entities (NER over rendered text), heading-to-content depth ratio, and whether the page is a single-topic page vs. a hub. Flag as `topical_depth: low/medium/high`; wire into the recommendations priority weighting. **Note:** don't claim topical authority as a citation guarantee — flag as "structural signal."

- 🎯 **Per-platform recommendation tagging** (`pipeline/goblin/nodes/recommend.py`) — ChatGPT drives 87.4% of AI referral traffic and cites pages that don't rank in Google's top 100 (80% of citations). Google AIO requires Page 1 first. The pipeline currently produces a monolithic rec list. Add a `platform` tag to each rec: `chatgpt` (freshness, structure, direct-answer frontloading), `google_aio` (authority, existing ranking, E-E-A-T), or `both`. The client report and AI-prompt artifact should present these in two lanes.

- 🏪 **Third-party platform presence check** (`pipeline/goblin/nodes/recon.py` or new `platforms_audit` node) — For B2B SaaS: ~82% of comparison queries cite G2/Capterra/TrustRadius/Gartner Peer Insights — not the client's own site. For finance: NerdWallet/comparison aggregators. For e-commerce: Amazon brand presence. The scan should check whether the client has a profile on the relevant platform tier for their ICP segment, and flag absence as a HIGH citation gap. Add `icp_segment` → `platform_checklist` mapping keyed off the `recon` node's `company_profile`.

- 🗄️ **Supabase + pgvector schema** (dashboard workstream) — The vault has a full proposed schema at `Prompt Goblin - Supabase + Vector DB Plan.md`: `clients`, `competitors`, `runs`, `citations`, `competitor_citations`, `page_chunks` (w/ `vector(1536)` + ivfflat index), `crawl_pages`, `recommendations`, `verification_results`. This is Phase 1 of the dashboard build — create committed migrations before any Next.js API routes. pgvector over cached `page_chunks` removes the hard dependency on live-fetch-only, improving reliability against WAF/JS-heavy blocks. Add RLS tests proving one client cannot read another client's rows, and seed fixtures where every sample metric is explicitly marked sample. **Prerequisite:** deploy the Next.js `web` cutover first; then provision a Supabase project and run migrations.

- 📅 **Freshness cadence check** (`functions/lib/hygiene.js` + `pipeline/goblin/nodes/seo_audit.py`) — Finance content cited by AI is updated within 30 days — freshness is the dominant lever in that vertical. Add a content-age signal: parse `<meta name="revised">` / `<time datetime>` / sitemap `<lastmod>` and flag if >30 days for finance-segment clients. Low effort, high signal for Finance ICP. Emit as a `FRESHNESS_STALE` finding with severity calibrated by segment.

- 📰 **Proprietary Citation Landscape Benchmark** (marketing + pipeline) — Run the pipeline on 10–20 sample domains per vertical (B2B SaaS, finance, legal, e-commerce, healthcare) and publish the results as a quarterly Prompt Goblin Citation Benchmark report. This is the dogfood loop that turns the tool into its own citation source — builds SEO/AEO authority for `promptgoblin.io` and gives sales a proprietary data asset. Publish as a `/benchmark` page (static, no auth) + a downloadable PDF. Honest-broker: clearly label sample methodology; no cherry-picking.

- 🎨 **Messaging: fabrication crisis hook** (copywriter gate) — Brief the `copywriter` to test the trust/verification angle on the hero sub-line and HowItWorks section copy: *"51% of AI citations are fabricated — ours are verified."* This wins with technical founders and marketing ops. Current site leads with visibility; verification is a sharper hook in the current trust-eroded market. Route through `integrity-reviewer` before shipping. Do NOT add stat to copy without the honest-broker hedge ("in independent investigations").

### Decision recorded (2026-06-06)
- **Scout $997/mo is validated below market.** Research floor is $1,500–$1,997 for founders; Scrunch starts at $299 (self-serve), Evertune $499, Bluefish $399, AirOps $500. Prompt Goblin is done-for-you, not self-serve — the pricing gap is intentional for first-client conversion. Plan to raise once 2 logos are landed.

## Decisions log

- 2026-06-05: **pricing → monthly + cancel-anytime**, then dropped to an **aggressive land-grab ladder: Scout $997 · Warband $3,500 · Warlord $9,500/mo** (owner). Intentionally below the research band ($1,500–$1,997 for founders) — owner is new to market and prioritizing first-client conversion + case studies over margin; raise once there are logos. Stripe Payment Links must be regenerated to match.
- 2026-06-05: **free-scan form reduced to domain + email**; competitor + tech-stack are now **auto-discovered** (recon node + backend HTML fingerprint), so the manual inputs were removed. Email kept (lead + delivery).
- 2026-06-05: **recon never overrides operator-provided competitors** (explicit always wins; auto-discovery caps at 2 and only fires when none given) — keeps all existing eval/tests stable and the honest-broker line intact.
- 2026-06-02: mobile fix handed from user → `design-system` agent (user request). Sequence: plan → mobile → verify → apply copy. Diagnosed via `:8127` preview (screenshots unusable — CRT/grain effects hang the headless renderer; used deterministic `eval`/`inspect` layout measurement instead).
- 2026-06-02: roster = full 9-role team (a11y folded into `qa`, security into `stack-auditor`; planner = main thread + this file).
- 2026-06-02: copy = draft now, apply after the user's mobile fix (avoid file clobber).
- Leads: widen categories (B2B SaaS + ecom + local high-LTV service + agencies); leads before own-site fixes.
- 2026-06-02: agreed work order after owner reviews the redesign docs → (1) owner review + hand `REDESIGN_BRIEF.md` to Claude Design, then (2) verify the backend (`functions/` Tier-2 + PostHog ingestion end-to-end), then (3) Pipeline task (b). Plus: `GEMINI_ASSET_PROMPTS.md` created for on-brand social-post asset generation.
